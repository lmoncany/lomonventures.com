export interface Project {
  id: string;
  title: string;
  description: string;
  icon: string;
  url: string;
}

export interface Profile {
  name: string;
  location: string;
  description: string;
  about: string;
  email: string;
}
